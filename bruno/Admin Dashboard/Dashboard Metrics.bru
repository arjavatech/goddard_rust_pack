meta {
  name: Dashboard Metrics
  type: http
  seq: 1
}

get {
  url: {{base_url}}/admin/dashboard-metrics?school_id={{school_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{admin_token}}
}

settings {
  encodeUrl: true
}
