meta {
  name: Get Parent by ID
  type: http
  seq: 1
}

get {
  url: {{base_url}}/parent/{{parent_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
}
