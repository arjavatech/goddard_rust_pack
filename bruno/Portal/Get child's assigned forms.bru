meta {
  name: Get child's assigned forms
  type: http
  seq: 9
}

get {
  url: {{base_url}}/parents/{{parent_id}}/children/{{child_id}}/forms
  body: none
  auth: inherit
}

headers {
  X-API-Key: {{api_key}}
}

settings {
  encodeUrl: true
}
