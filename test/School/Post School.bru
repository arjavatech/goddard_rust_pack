meta {
  name: Post School
  type: http
  seq: 2
}

post {
  url: {{base_url}}/schools
  body: json
  auth: inherit
}

headers {
  X-API-Key: test-owner-key-2024
}

body:json {
  {
        "name": "Mani's School, Chennai",
        "subdomain": "Chennaixff",
        "settings": {
          "timezone": "America/New_York",
          "enrollment_capacity": 200,
          "age_groups": ["infants", "toddlers", "preschool", "pre-k"]
  }
  }
}

settings {
  encodeUrl: true
}
